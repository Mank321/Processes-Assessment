WORLD RENDERING PROBLEMS
14-15 June - When making the scene changing program, I wrote up a function to load the tutorial,
and a seperate function to load the market. I later found this to be repetitive as
I would need to create a function for each level. So I decided to compress the functions
into one that could handle everything. This lead me to creating a class each for the tutorial,
market, and monster levels, which should allow me to turn everything on and off easily. 
When switching scenes, I got an error that manager.new_scene didn't exist. I fixed this by
setting market_world and tutorial_world to None at the very begining, then in the switch_scenes
function, I checked if the new_scene was None, then checked the old_scene's gate name to discover
what the new_scene should be, then initialized the correct scene.
I had to change the entities lists from game_state to the new class, in the collision detectors.

It went from:
def load_market(self):
        if self.game_state.location == 'tutorial':
            for entity in self.game_state.tutorial_entities:
                entity.disable()
            for entity in self.game_state.tutorial_colliders:
                entity.disable()
        if hasattr(self.game_state, 'market_entities'):
            for entity in self.game_state.market_entities:
                entity.enable()
            for entity in self.game_state.market_colliders:
                entity.enable()
        else:
            create_market_world(self.game_state)
        print_on_screen('-Market-', position=(-0.1,0.45), scale=3, duration=3)
        self.game_state.location = 'market'
        self.player.position = (0,0,0)

To:
def switch_scenes(self, new_scene, old_scene):
        """."""
        scene = old_scene.gate.name.split('.')[1]

        if new_scene == None:
            if scene == 'market':
                self.market_world = MarketWorld(self.game_state)
                new_scene = self.market_world

        new_scene.enable()
        for entity in new_scene.entities:
            entity.enable()
        for entity in new_scene.colliders:
            entity.enable()
        old_scene.disable()
        for entity in old_scene.entities:
            entity.disable()
        for entity in old_scene.colliders:
            entity.disable()
        
        print_on_screen(f'--{scene.title()}--', position=(-0.1,0.45), scale=3, duration=2)
        self.game_state.location = scene
        self.player.position = (0,0,0)

-
FIXED: When I added a second gate in the market that would lead to the monsters, I had to rename 
the gate since there were multiple. This led to the switch_scenes method no longer working. To fix
it, I changed the name of the gates from gate.{destination}, to gate.{origin}.{distination}. I then
changed the method to set old_scene to the variable containing the old world using a dictionary, 
and set the new_scene to a variable containing the new world. But I had to check if the new world
had been created yet.
-
FIXED: When I added the two gates to the LevelCreator class, I had to figure out where the gates
went, since the class could be initialized as any level and needed a gate that returned the 
player to the previous world, and one that sent them to the next world. To fix this, I checked 
the player location, then got its index in the locations list and added/subtracted 1 to get the
new worlds. But the next world would take me to the first world, and the previous gate took me to
the tutorial. After debugging, I realised I was setting the player location after I'd created the
class. To fix this, I simply updated the player location before I created the world.
-
FIXED: When I walked up to the gate on a good computer with debug mode on, I'd inch close to the
gate to test it. It went red when the orange cube tuched it, but I didn't teleport. I needed
my tiny hitbox to tuch the gate for me to teleport. I fixed this by increasing the size of my 
hitbox to that of the orange cube. Upon further inspection, I realised that tiny hitbox wasn't a
hitbox, but actually the raycasting. I set the distance from 0.1 to 0.5 and it worked perfectly.
-
FIXED: When I was replacing the portal with a more recognisable model, I had to spend a while
figuring out how to put multiple parts of the portal into the game. Finding the right textures, 
trying to get normal maps to work. Eventually I gave up on the normal maps and tried to get the 
scale and positioning right. I got the entire portal done except for the vertex, and after spending
hours on trying to position it correctly, I tried: changing its location and origin point in the
blender file, trying to change each axis individually in code, trying to change its origin point
position, switching from creating the object in super initializer to standard initialization,
creating a vertext entity outside the class. Eventually, the last one worked, but then I had to
integrate it into the gate class. To get its scale right I had to create an anchor object so it
wouldn't look warped. After all this, I finally figured out that I was trying to change the
position_y, position_x value, when I should've been trying to change the y, x value.
-
FIXED: When I was getting the gates to only teleport the player after they'd defeated the boss, I
quickly got the animation sequence working, but then struggled to deny the player if the boss wasn't
defeated. I tried adding a complete variable into the gate class, then using hit_info to extracted
it to determine if the player should switch scenes. But it turns out hit_info can't see custom 
variables, so I had to put it inside the name. While I was attempting this, I realised I could just
disable and enable the gate's collision_box based on complete. This worked fine, except for the fact
that when disbaled, the player could walk right through the gate, which made me scrap the idea and went
back to the name approach. When I changed self.gate.name, I forgot to realise that the raycast was
actually hiting the collision_box entity, so after a bit of debugging I changed the collision_box
name to gate.location.complete, set the raycast to look for the complete, and it worked.

WORLD CREATION PROBLEMS
FIXED: When creating the forest in the tutorial area, I started with a massive list containing the
pre-chosen position of each tree. Then I looped through the list to plant each tree. This was
incredibly inefficient, so I create a 2d map array which had the entire forest plotted out in
a dozen lines. I initially filled the entire area in with over a thousand trees, but this tanked
the framerate down to 20fps, so I knew I couldn't do it that way without breaking a requirement.
Instead, I shrunk the forest down to a minimum size with just having the trees as border walls,
and shrunk the main perimeter wall to fit the new forest size. It took a bit of time to get the
exact coordinates right and the tree spacing and map array. 

COMBAT SYSTEM PROBLEMS
FIXED: When creating the combat system, I added in the monster, gave it a hitbox, damage, and
health, and gave it movement and distance checks, allowing it to move up to the player and attack
them when close enough, taking away 1 health point per second. But when I tried to have the player
attack the monster in retaliation, nothing happened. After a bit of debugging, I realised I was
checking to see if the mouse.hovered_entity had health. I initially thought the entity was the
monster itself, but it was actually the collision box that the player was hitting, as I discovered
when I gave the collision box a name and printed it when attacking. I thought of adding the monster
stats right into the collision box entities, but that felt wrong, so I attempted to create an
on_click method for the monster, but that ended up not being called, or calling as soon as the
player was in range. I thought about going back to the collision box idea, but thought of an idea
to fix the monster not being instantly destroyed by only calling the on_click method when the
player actually clicked on the monster. I still ran into a problem where the code was trying to
find a monster that had been destroyed when checking for collisions. To fix this, I decided to 
allow the player to walk through monsters, meaning I didn't need to check their hitbox.
-
FIXED: When trying to use a gate, since the monster was destroyed, it couldn't be disabled, 
creating errors. To fix this, I tried changing the switch_scenes method from using an entities 
list given by TutorialWorld, to going through every child of TutorialWorld itself, and going
through every child of those children to catch the collision boxes. This allowed me to go through
the gate, but didn't disable anything. Instead, I parented everything in the world to the 
TutorialWorld entity, allowing me to disable everything with one line.
-
FIXED: When I started adding in more monsters, I added medusa, which worked fine, then I added the
basilisk, which was tiny. I tried changing the scale, but that made the collision box grow massively. 
I eventually figured out that the collision box had hard-coded position and scale variables that 
were designed for the centaur. To fix this, I added in collider_scale and collider_position into
the monster stats.
-
When I leveled up while being attacked by a monster, or when I leveled before ever being attacked,
I would become permenantly invulnrable.

PLAYER SYSTEMS
FIXED: When building the levelup system, I created a healthbar in the UI, and created a method to
levelup. When leveling up, the player's health returns to max, and when printed out, it does. But on
screen, and when fighting a monster, the UI healthbar doesn't change at all, neither the value or 
max_value. Through debugging, I discovered the problem came from the invoke() function from the
monster class that deals damage to the player. I intially replaced it with a simple 
health -= damage * time.dt, but this resulted in many decimal places. So in the ui_state update
method, I simply made it display a rounded number.
-
NOT-FIXED: When building the teleport feature, I initially started out as having a button to
activate it, but then realised the mouse was locked so the player couldn't press any buttons. I
fixed this by replacing the button with a keybind. I will need to make the player aware of it
though. When I got to teleporting, I tried teleporting to a world I was already in, and everything
disappeared. I will need to disable the button that leads to the world the player is currently in.

COMPUTER/DEVICE AND SOFTWARE PROBLEMS
NOT-FIXED: While working on the game, I would sometimes change computers. I'd build a feature,
save it, then pull it on another computer, and the feature would be slightly different(wall in
different position, portals and stalls rotated 90 degrees in market), but when I returned to the 
computer I made the feature on, even after changing nothing, or 'fixing' it to make it return to 
normal, the feature returned to what it was, even after saving properly.
-
NOT-CRITICAL: Although I often did work on VSC, I would sometimes get swap to thonny to debug. This
would however sometimes cause problems. I would get crashes in thonny I never got in VSC. For 
example, when building the combat system, and the hit boxes were destroyed, thonny would raise an
error in the player for loop where it checked every collider in the world, monster included which 
no longer existed. But VSC never raised such an error.
-
FIXED: I was working on the game one day at school, then went home and worked on it on my PC. As 
soon as I tried to go to the market, the game crashed, stating the stall_texture could not load
texture. After much debugging and experimenting with paths, I realised the prop texture, which was
located in the exact folder, was working fine. So I tried saving as the texture as something new.
When I plugged the new texture back into the game, I got the exact same error. But then I saw the
error: ':pnmimage:png(error): IDAT: CRC error'. This told me the image was corrupted. Going back, I
noticed on some of the image thumbnails that the image was sometimes displayed as having a corrupted
mess of green pixels. Since it was working fine on the previous devices, my theory is that this was
anchorpoints doing. To fix it, I had to go and redownload the image from where I found it.
-
NOT_CRITICAL: Sometimes I would spawn in facing a slightly different direction, sometimes not.

USER INTERFACE PROBLEMS
FIXED: When coding the UI, I wanted to display the health as a resourcebar on screen, so I used the built-in
ursina healthbar prefab. Initially, so that the player took damage every second, the damage was multiplied
by time.dt, but that resulted in multiple decimal places. So I created an invoke function delayed by a second
This worked to a point, as there was a delay for the healthbar updating - the bar itself would only
visually update when the player was moving, so the gold bar would start at 10 and work down to 0, and 
the health bar would say 2 but look like it was 15. To fix this, I created my own healthbar class. It 
took multiple attempts, and many trials such as getting everything overlapping correctly, I even thought
about returning to the prefab, but I figured it out
-
FIXED: When building the death screen, I started by created the menu from scratch, but then realised I could
create a new instance of the main menu class and call is death screen. I added in some extra parameters
so it would have different wording than the main menu, and set the revive button to call player.death().
I had to figure out how to get the background ontop of the other resource bars, for which I settled
with giving it a higher z. I then ran into a problem in player.death() where the death screen couldn't
be disabled and returned an Attribute error. To fix this, I did some debugging, and realised I was
trying to disable the function death_screen(), instead of the menu death_menu. I then ran into a 
problem where if the xp was higher than 0, the death screen wouldnt appear. It also wouldn't appear a second
time. I eventually fixed this by initializing the menu at the start without a start function, then
assigning it the first time the player died. Another bug appeared because I wanted to pause the game
when the player died, so I had to apply ignore_paused to the buttons so they could be pressed.